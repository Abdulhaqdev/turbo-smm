import { ReactNode } from 'react'
export interface CountUpAnimationProps {
	end: number;
	duration?: number;
	prefix?: string;
	suffix?: string;
}
export interface ChilndProps {
	children : ReactNode
}